# Only needed when the dashboard is deployed separately from the cache
# node (e.g. dashboard on Vercel, node on Render/Railway/Fly/a VPS).
# Leave unset for local dev — it defaults to the /api proxy in vite.config.ts.
#
# VITE_API_BASE=https://your-inkcache-node.example.com
