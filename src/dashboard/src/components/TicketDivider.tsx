/** A row of punched dots, ticket-stub style — a section break with some
    shape to it instead of a plain hairline or empty gap. */
export function TicketDivider() {
  return <div aria-hidden className="retro-perf" />;
}
