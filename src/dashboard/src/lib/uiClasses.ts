/**
 * Shared base styling for the small bracketed/boxed controls (Button,
 * Toggle). Kept in one place so the two don't drift apart if the shape
 * language changes — they're meant to look like the same control family.
 */
export const CONTROL_BASE =
  "cursor-pointer border border-ghost px-2 py-1 text-[10px] tracking-widest uppercase";
