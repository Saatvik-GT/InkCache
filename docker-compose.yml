services:
  node:
    build: .
    ports:
      - "8080:8080"
    environment:
      INKCACHE_PORT: 8080
      INKCACHE_NODE_ID: node-1
      # INKCACHE_CORS_ORIGIN: https://your-dashboard.example.com
